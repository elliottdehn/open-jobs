# secrets
oai_key.txt
*.key
backend/.dev.vars
# local working data
work/
backend/export/
backend/export-*/
backend/logs/
# build/deps
backend/node_modules/
backend/.wrangler/
__pycache__/
.DS_Store
other/

# per-user Claude Code permissions (the /ideas opt-in rule lives here, never in the repo)
.claude/settings.local.json

# Worker admin token (also set as a wrangler secret)
backend/admin_token.txt
