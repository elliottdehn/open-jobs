/**
 * For more details on how to configure Wrangler, refer to:
 * https://developers.cloudflare.com/workers/wrangler/configuration/
 */
{
	"$schema": "node_modules/wrangler/config-schema.json",
	"name": "backend",
	"main": "src/index.ts",
	"compatibility_date": "2026-08-26",
	"migrations": [
		{ "tag": "v1", "new_sqlite_classes": ["MyDurableObject"] },
		{ "tag": "v2", "deleted_classes": ["MyDurableObject"], "new_sqlite_classes": ["Board", "Registry"] },
		{ "tag": "v3", "new_sqlite_classes": ["RateLimit"] },
		{ "tag": "v4", "new_sqlite_classes": ["Budget"] },
		{ "tag": "v5", "new_sqlite_classes": ["Lock"] },
		{ "tag": "v6", "new_sqlite_classes": ["Stats"] },
		{ "tag": "v7", "new_sqlite_classes": ["Dedupe"] },
		{ "tag": "v8", "new_sqlite_classes": ["Consolidate"] }
	],
	// The Worker bound to itself: the consolidation container reaches the API through an outbound handler on
	// http://worker.internal (containers cannot reach *.workers.dev hostnames), which forwards over this binding.
	"services": [{ "binding": "SELF", "service": "backend" }],
	// The consolidation container (CONTAINER.md): the same image as the laptop (backend/Dockerfile, built for
	// linux/amd64 and pushed by `wrangler deploy`), the largest self-serve instance, one at a time.
	"containers": [
		{ "class_name": "Consolidate", "image": "./Dockerfile", "image_build_context": "..", "instance_type": "standard-4", "max_instances": 1 }
	],
	"durable_objects": {
		"bindings": [
			{ "class_name": "Board", "name": "BOARD" },
			{ "class_name": "Registry", "name": "REGISTRY" },
			{ "class_name": "RateLimit", "name": "RATELIMIT" },
			{ "class_name": "Budget", "name": "BUDGET" },
			{ "class_name": "Lock", "name": "LOCK" },
			{ "class_name": "Stats", "name": "STATS" },
			{ "class_name": "Dedupe", "name": "DEDUPE" },
			{ "class_name": "Consolidate", "name": "CONSOLIDATE" }
		]
	},
	// Static data for the local-first client: tree manifest, group files, parquet. Served via GET /data/*.
	"r2_buckets": [{ "binding": "DATA", "bucket_name": "jobscream-data" }],
	// The search page (../site/index.html, one self-contained file). Served at / ; every API route is
	// listed here so the Worker sees it first and the assets layer never shadows an endpoint.
	"assets": { "directory": "../site", "binding": "ASSETS", "run_worker_first": ["/", "/status", "/probe", "/embed", "/jd", "/enrich", "/enrich/*", "/ideas", "/data/*", "/ats", "/sync", "/sync/*", "/snapshots", "/fetch-all", "/backfill", "/boards/*", "/jobs/*", "/export/*", "/comeet/*"] },
	"triggers": {
		// Daily sweep that (re)arms every board's alarm. Boards fetch at their own fixed slot.
		"crons": ["0 0 * * *"]
	},
	"vars": {
		// ADMIN_TOKEN is a *secret* (`wrangler secret put ADMIN_TOKEN`), deliberately not a var: a var here would shadow the secret and the admin endpoints fail closed without it.
		// "on" = resolve the company behind every board automatically after its first successful fetch
		// (one OpenAI Responses call with web_search per board, ~40k live boards). "off" = only via
		// POST /boards/:ats/:slug/enrich-board.
		"BOARD_ENRICH": "off",
		// "on" = embed every live job once (text-embedding-3-small, 768 dims) after its detail fetch.
		"EMBED": "on",
		// "on" = run the structured job extraction automatically for every new job (see FIELDS.md §2 cost).
		// "off" = only lazily via POST /jobs/enrich.
		"JOB_ENRICH": "off",
		// POST /embed per-IP limit
		// Public POST /enrich: per-IP USD caps (actual token + web-search cost; cached results are free)
		"ENRICH_HOUR_USD": "5",
		"ENRICH_DAY_USD": "50",
		"EMBED_RATE_LIMIT": "10",
		"EMBED_RATE_WINDOW_MS": "600000",
		"IDEAS_MENTION": "U0AA4FAAC6L"
	},
	// Export pages with JD bodies + vectors are ~100 MB of JSON; the default 30 s CPU limit cuts them off.
	"limits": { "cpu_ms": 300000 },
	"observability": {
		"enabled": true
	},
	"upload_source_maps": true,
	"compatibility_flags": [
		"nodejs_compat"
	]
	/**
	 * Smart Placement
	 * https://developers.cloudflare.com/workers/configuration/smart-placement/#smart-placement
	 */
	// "placement": {  "mode": "smart" }
	/**
	 * Bindings
	 * Bindings allow your Worker to interact with resources on the Cloudflare Developer Platform, including
	 * databases, object storage, AI inference, real-time communication and more.
	 * https://developers.cloudflare.com/workers/runtime-apis/bindings/
	 */
	/**
	 * Environment Variables
	 * https://developers.cloudflare.com/workers/wrangler/configuration/#environment-variables
	 * Note: Use secrets to store sensitive data.
	 * https://developers.cloudflare.com/workers/configuration/secrets/
	 */
	// "vars": {  "MY_VARIABLE": "production_value" }
	/**
	 * Static Assets
	 * https://developers.cloudflare.com/workers/static-assets/binding/
	 */
	// "assets": {  "directory": "./public/",  "binding": "ASSETS" }
	/**
	 * Service Bindings (communicate between multiple Workers)
	 * https://developers.cloudflare.com/workers/wrangler/configuration/#service-bindings
	 */
	// "services": [  {   "binding": "MY_SERVICE",   "service": "my-service"  } ]
}